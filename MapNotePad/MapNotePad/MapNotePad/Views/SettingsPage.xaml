<?xml version="1.0" encoding="utf-8" ?>
<view:BaseContentPage  xmlns:view="clr-namespace:MapNotePad.Views" 
                       xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:res="clr-namespace:MapNotePad.Helpers;assembly=MapNotePad"
             x:Class="MapNotePad.Views.SettingsPage">
    <view:BaseContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" VerticalOptions="CenterAndExpand"  HorizontalOptions="FillAndExpand">
                <Image Source="pic_enter_page.png" HeightRequest="190" WidthRequest="200"/>
                <Label Text="{res:Translate MapNotepad}" Style="{StaticResource baseMapLabel}"/>
            </StackLayout>
            <StackLayout Grid.Row="1" Padding="20,0">
                <Button Text="Log in" Margin="0,0,0,20" Style="{StaticResource baseButton}" Command="{Binding LogInCommand}" />
                <Button Text="Create account" Style="{StaticResource inverseButtonStyle}" Command="{Binding RegisterCommand}" />
            </StackLayout>
        </Grid>
    </view:BaseContentPage.Content>
</view:BaseContentPage>
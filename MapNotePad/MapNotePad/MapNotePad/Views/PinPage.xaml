<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             x:Class="MapNotePad.Views.PinPage"
             BackgroundColor="Transparent">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" HorizontalOptions="FillAndExpand"  VerticalOptions="FillAndExpand">
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Command="{prism:GoBack}" />
                </StackLayout.GestureRecognizers>
            </StackLayout>
            <Button Text="Pinnnnnnnnnnnnnnnnnnn" FontSize="Large" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Command="{prism:GoBack}" BackgroundColor="Transparent" />
        </Grid>
    </ContentPage.Content>
</ContentPage>